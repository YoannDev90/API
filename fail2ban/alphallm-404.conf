# Filtre pour détection des erreurs 404
# Détecte les tentatives d'accès à des endpoints inexistants
# Bannit l'IP après 3 erreurs 404

[Definition]
failregex = ^.*Request: .* from <HOST> - Response: 404.*$
ignoreregex =
